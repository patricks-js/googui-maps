services:
  maps-mongodb:
    image: bitnami/mongodb:latest
    container_name: maps-mongodb
    restart: always
    env_file:
      - .env.docker
    ports:
      - 27017:27017
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
    driver: local
